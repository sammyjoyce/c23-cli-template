/*
 * Internal helpers shared between TUI implementation files.
 */

#pragma once

#include "tui.h"

void tui_internal_set_background_window(tui_window_t *window);
void tui_internal_clear_background_window(void);
